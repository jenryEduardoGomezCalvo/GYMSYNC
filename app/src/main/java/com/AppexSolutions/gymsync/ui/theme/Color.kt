package com.AppexSolutions.gymsync.ui.theme

import androidx.compose.ui.graphics.Color

// Paleta principal - Azul Marino y Negro para estética fitness premium
val NavyBlue = Color(0xFF0A1628)  // Azul marino principal
val DarkNavy = Color(0xFF050C18)  // Negro azulado
val MidnightBlue = Color(0xFF1A2332)  // Azul medianoche
val SteelBlue = Color(0xFF2D3748)   // Azul acero
val ElectricBlue = Color(0xFF3B82F6) // Azul eléctrico para CTAs
val BrightBlue = Color(0xFF60A5FA)   // Azul brillante para acentos

// Colores de superficie y fondo
val SurfaceDark = Color(0xFF1F2937)  // Superficie oscura
val CardBackground = Color(0xFF111827) // Fondo de cards
val InputBackground = Color(0xFF374151) // Fondo de inputs

// Colores de texto
val TextPrimary = Color(0xFFFFFFFF)   // Blanco puro
val TextSecondary = Color(0xFFD1D5DB) // Gris claro
val TextMuted = Color(0xFF9CA3AF)     // Gris muted

// Estados y feedback
val SuccessGreen = Color(0xFF10B981)  // Verde éxito
val ErrorRed = Color(0xFFEF4444)      // Rojo error
val WarningAmber = Color(0xFFF59E0B)  // Ámbar warning

// Gradientes para efectos premium
val GradientStart = NavyBlue
val GradientEnd = DarkNavy

// Mantener compatibilidad con Material 3 (no usados)
val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)
val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)